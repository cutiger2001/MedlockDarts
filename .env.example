# MS-SQL Server connection
DB_SERVER=localhost
DB_PORT=1433
DB_INSTANCE=SQLEXPRESS
DB_USER=DartsAdmin
DB_PASSWORD=180Allday!
DB_NAME=DartsLeague

# Server
SERVER_PORT=3001
NODE_ENV=production

# Client URL (for CORS — not used in production, client served from same origin)
CLIENT_URL=http://localhost:5173
